<div class="edit-post">
    <h3> Edit Post </h3>
    <form class="form-edit-post" id="edit-post-form">
        <input type="hidden" name="post-id" value="{{post.id}}" />
        <div class="form-group">
                <label class="form-label" for="post-title">Title</label>
                <input type="text" name="post-title" value="{{post.title}}" class="form-input" />
        </div>
        <div class="form-group">
            <label class="form-label" for="post-body">Content</label>
            <textarea name="post-body" class="form-input">{{post.post}}</textarea>
        </div>
        
    <button type="submit" class="btn">Update</button>
    <button type="button" id="delete-btn" class="btn">Delete</button>
    </form>
</div>

<script src="/js/edit-post.js"></script>
